/* import { queryExcute } from "../db";

export async function GET(req,{params}){
    const id= await queryExcute ('SELECT latitude from itemtable where latitude=?',[params.num])
    return Response.json(id);
} */